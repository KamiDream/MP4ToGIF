[project]
name = "mp4togif"
version = "0.1.0"
description = "将 MP4 视频文件转换为 GIF 动图的命令行工具"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "auto-py-to-exe>=2.50.1",
    "moviepy>=2.1.0",
]
